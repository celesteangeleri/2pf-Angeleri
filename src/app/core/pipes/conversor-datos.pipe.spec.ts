import { ConversorDatosPipe } from './conversor-datos.pipe';

describe('ConversorDatosPipe', () => {
  it('create an instance', () => {
    const pipe = new ConversorDatosPipe();
    expect(pipe).toBeTruthy();
  });
});
